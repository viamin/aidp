{
  // markdownlint-cli2 configuration
  // See: https://github.com/DavidAnson/markdownlint-cli2

  // Use the existing .markdownlint.yml config file for rules
  "config": ".markdownlint.yml",

  // Files to ignore (similar to .markdownlintignore)
  "ignores": [
    "CHANGELOG.md",
    "tmp/**",
    "vendor/bundle/**",
    ".aidp/**"
  ]
}
